<x-layouts.app title="Team"><div class="page-head"><div><p class="eyebrow">Workspace access</p><h1>Team members</h1><p>Invite colleagues, assign appropriate roles, and manage workspace access.</p></div></div>@if(session('invitation_url'))<div class="alert success"><strong>Invitation created</strong><p>Copy and share this private link with the invited person:</p><code>{{ session('invitation_url') }}</code></div>@endif<section class="detail-grid"><article class="panel"><h2>Invite a team member</h2><form method="POST" action="{{ route('team.invite') }}" class="form-stack">@csrf<label>Email address<input type="email" name="email" required></label><label>Workspace role<select name="role">@foreach($roles as $role)<option value="{{ $role->value }}">{{ str_replace('_',' ',ucfirst($role->value)) }}</option>@endforeach</select></label><button class="button primary">Create invitation</button></form><p class="muted">The invitation expires after seven days. Copy the generated link and share it securely.</p></article><article class="panel"><h2>Active members</h2><div class="member-list">@foreach($members as $member)<div><span class="avatar">{{ strtoupper(substr($member->name,0,1)) }}</span><div><strong>{{ $member->name }}</strong><small>{{ $member->email }} · {{ ucfirst($member->pivot->role) }}</small></div>@if($member->id!==auth()->id() && $member->pivot->is_active)<form method="POST" action="{{ route('team.deactivate',$member->pivot->id) }}">@csrf @method('PATCH')<button class="button secondary">Deactivate access</button></form>@endif</div>@endforeach</div></article></section><article class="data-panel"><div class="panel-copy"><h2>Pending invitations</h2></div>@forelse($invitations as $invitation)<div class="invitation-row"><span>{{ $invitation->email }}</span><span>{{ ucfirst($invitation->role->value) }}</span><small>Expires {{ $invitation->expires_at->diffForHumans() }}</small></div>@empty<div class="empty-state compact"><p>No pending invitations.</p></div>@endforelse</article></x-layouts.app>
