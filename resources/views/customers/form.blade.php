<x-layouts.app :title="$customer->exists ? 'Edit customer' : 'Add customer'"><div class="page-head"><div><p class="eyebrow">Customers</p><h1>{{ $customer->exists ? 'Edit customer' : 'Add customer' }}</h1><p>Keep billing and contact information ready for quotation creation.</p></div></div><form class="form-card" method="POST" action="{{ $customer->exists ? route('customers.update',$customer) : route('customers.store') }}">@csrf @if($customer->exists)@method('PUT')@endif<div class="form-grid"><label>Customer type<select name="type"><option value="company" @selected(old('type',$customer->type)==='company')>Company</option><option value="individual" @selected(old('type',$customer->type)==='individual')>Individual</option></select></label><label>Name<input name="name" value="{{ old('name',$customer->name) }}" required>@error('name')<span class="field-error">{{ $message }}</span>@enderror</label><label>Email<input type="email" name="email" value="{{ old('email',$customer->email) }}"></label><label>Phone / WhatsApp<input name="phone" value="{{ old('phone',$customer->phone) }}"></label><label>Tax ID<input name="tax_id" value="{{ old('tax_id',$customer->tax_id) }}"></label><label>Currency<input name="currency" maxlength="3" value="{{ old('currency',$customer->currency ?: $activeWorkspace->currency) }}"></label><label>Locale<input name="locale" value="{{ old('locale',$customer->locale ?: 'en') }}" required></label><label>Status<select name="status"><option value="active" @selected(old('status',$customer->status)==='active')>Active</option><option value="inactive" @selected(old('status',$customer->status)==='inactive')>Inactive</option></select></label><label class="span-2">Billing address<textarea name="billing_address">{{ old('billing_address',$customer->billing_address) }}</textarea></label><label class="span-2">Internal notes<textarea name="notes">{{ old('notes',$customer->notes) }}</textarea></label></div><div class="form-actions"><a class="button secondary" href="{{ route('customers.index') }}">Cancel</a><button class="button primary">Save customer</button></div></form></x-layouts.app>
