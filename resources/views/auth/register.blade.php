<x-layouts.guest title="Create workspace — QuoteFlow AI"><div class="auth-card"><p class="eyebrow">Get started</p><h2>Create your workspace</h2><p class="muted">Set up the secure foundation for your quotation workflow.</p><form method="POST" action="{{ url('/register') }}" class="form-stack">@csrf<label>Your name<input name="name" value="{{ old('name') }}" autocomplete="name" required>@error('name')<span class="field-error">{{ $message }}</span>@enderror</label><label>Work email<input name="email" type="email" value="{{ old('email') }}" autocomplete="email" required>@error('email')<span class="field-error">{{ $message }}</span>@enderror</label><label>Workspace name<input name="workspace_name" value="{{ old('workspace_name') }}" required>@error('workspace_name')<span class="field-error">{{ $message }}</span>@enderror</label><label>Password <small>At least 12 characters</small><input name="password" type="password" autocomplete="new-password" required>@error('password')<span class="field-error">{{ $message }}</span>@enderror</label><label>Confirm password<input name="password_confirmation" type="password" autocomplete="new-password" required></label><button class="button primary" type="submit">Create workspace</button></form><p class="auth-foot">Already registered? <a href="{{ route('login') }}">Sign in</a></p></div></x-layouts.guest>
