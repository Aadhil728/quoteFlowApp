<x-layouts.guest title="Verify email — QuoteFlow AI"><div class="auth-card"><p class="eyebrow">One more step</p><h2>Verify your email address</h2>@if(config('mail.default')==='log')<p class="muted">Email delivery is in local-development mode. Verification messages are written to the application log until SMTP is configured.</p>@else<p class="muted">Open the verification link sent to your email address. Check your spam folder if it does not arrive within a few minutes.</p>@endif @if(session('status'))<div class="alert success" role="status">A new verification link has been prepared.</div>@endif<div class="auth-actions"><form method="POST" action="{{ route('verification.send') }}">@csrf<button class="button primary">Send a new link</button></form><form method="POST" action="{{ route('logout') }}">@csrf<button class="button secondary">Sign out</button></form></div></div></x-layouts.guest>
