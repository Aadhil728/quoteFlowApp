<x-layouts.guest title="Sign in — QuoteFlow AI"><div class="auth-card"><p class="eyebrow">Welcome back</p><h2>Sign in to your workspace</h2><p class="muted">Continue managing quotations, approvals, and payments.</p>@if(session('status'))<div class="alert success" role="status">{{ session('status') }}</div>@endif<form method="POST" action="{{ url('/login') }}" class="form-stack">@csrf<label>Email address<input name="email" type="email" value="{{ old('email') }}" autocomplete="email" required autofocus>@error('email')<span class="field-error">{{ $message }}</span>@enderror</label><label>Password<input name="password" type="password" autocomplete="current-password" required></label><div class="form-row"><label class="check"><input type="checkbox" name="remember"> Remember me</label><a href="{{ route('password.request') }}">Forgot password?</a></div><button class="button primary" type="submit">Sign in</button></form><p class="auth-foot">New to QuoteFlow? <a href="{{ route('register') }}">Create a workspace</a></p></div></x-layouts.guest>
