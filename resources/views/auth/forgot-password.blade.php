<x-layouts.guest title="Reset password — QuoteFlow AI"><div class="auth-card"><p class="eyebrow">Account recovery</p><h2>Reset your password</h2><p class="muted">Enter your email and we will send a secure reset link if the account exists.</p>@if(session('status'))<div class="alert success" role="status">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('password.email') }}" class="form-stack">@csrf<label>Email address<input name="email" type="email" value="{{ old('email') }}" required autofocus>@error('email')<span class="field-error">{{ $message }}</span>@enderror</label><button class="button primary" type="submit">Send reset link</button></form><p class="auth-foot"><a href="{{ route('login') }}">Back to sign in</a></p></div></x-layouts.guest>
