<x-layouts.guest title="Install QuoteFlow AI"><div class="auth-card"><p class="eyebrow">System installer</p><h2>Prepare QuoteFlow AI</h2><p class="muted">Confirm server readiness, choose the operating mode, and create the first administrator.</p><div class="checks">@foreach($checks as $label=>$passed)<div><span class="{{ $passed ? 'check-pass' : 'check-fail' }}">{{ $passed ? '✓' : '×' }}</span><span>{{ $label }}</span></div>@endforeach</div>@if(in_array(false,$checks,true))<div class="alert danger" role="alert">Resolve failed requirements before continuing.</div>@else<form method="POST" action="{{ url('/install') }}" class="form-stack">@csrf<label>Operating mode<select name="mode"><option value="single_business">Single business</option><option value="saas">SaaS platform</option></select></label><label>Workspace name<input name="workspace_name" required></label><label>Administrator name<input name="name" required></label><label>Administrator email<input type="email" name="email" required></label><label>Password<input type="password" name="password" minlength="12" required></label><label>Confirm password<input type="password" name="password_confirmation" required></label><button class="button primary">Install QuoteFlow AI</button></form>@endif</div></x-layouts.guest>
