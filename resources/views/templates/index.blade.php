<x-layouts.app title="Quotation templates"><div class="page-head"><div><p class="eyebrow">Reusable documents</p><h1>Quotation templates</h1><p>Original layouts with versioned design settings. Duplicate one before tailoring it to a specialized workflow.</p></div><a class="button secondary" href="{{ route('quotations.create') }}">New quotation</a></div><div class="template-grid">@foreach($templates as $template)<article class="template-card"><div class="template-thumb style-{{ $template->style }}"><span>{{ strtoupper(substr($template->style,0,1)) }}</span><div><i></i><i></i><i></i></div></div><p class="eyebrow">{{ $template->style }} · v{{ $template->versions->first()?->version ?? 1 }}</p><h2>{{ $template->name }}</h2><div class="card-actions"><a class="button secondary" href="{{ route('templates.preview',$template) }}">Preview</a><form method="POST" action="{{ route('templates.duplicate',$template) }}">@csrf<button class="button primary">Duplicate</button></form></div></article>@endforeach</div></x-layouts.app>
