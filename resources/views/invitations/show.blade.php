<x-layouts.guest title="Workspace invitation — QuoteFlow AI"><div class="auth-card"><p class="eyebrow">Workspace invitation</p><h2>You have been invited</h2><p class="muted">Join the workspace as <strong>{{ ucfirst($invitation->role->value) }}</strong>. This secure invitation expires {{ $invitation->expires_at->diffForHumans() }}.</p>@auth@if(strcasecmp(auth()->user()->email,$invitation->email)===0)<form method="POST" action="{{ route('invitations.accept',$token) }}">@csrf<button class="button primary">Accept invitation</button></form>@else<div class="alert danger">Sign in with {{ $invitation->email }} to accept.</div>@endif @else<a class="button primary" href="{{ route('login') }}">Sign in to accept</a>@endauth</div></x-layouts.guest>
